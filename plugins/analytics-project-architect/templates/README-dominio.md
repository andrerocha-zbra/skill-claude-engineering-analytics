# {{DOMINIO}} — {{tecnologia}}

{{1 frase: o que este domínio/produto entrega.}}

## Estrutura

- `<pagina-ou-entidade>/especificacao.md` — especificação durável por página/entidade (fonte de verdade da entrega)
- `{{Projeto}}.pbip` — projeto Power BI (`.Report/` + `.SemanticModel/`)
- `docs/` — handoff, guia de apresentação, doc gerada, backlog
- `tasks/TASK-NNN__slug/` — tarefas (pasta única e top-level na raiz do projeto, não mais por domínio)

## Estado

Ver `PROJETO.md` na raiz (log vivo + estado por fase).

## Como retomar

1. Ler `docs/HANDOFF.md`.
2. Conferir o mapa Necessidade→Solução no `PROJETO.md`.
3. Seguir a próxima task em `tasks/` (raiz do projeto).
